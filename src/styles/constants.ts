export const HEADER_HEIGHT = 72;

export const enum Breakpoint {
  MINIMUM = 360,
  S = 480,
  M = 768,
  L = 1024,
  XL = 1200,
}

export const enum ZIndex {
  BACKGROUND = -1,
  DEFAULT = 1,
  TOGGLE = 100,
  HOVER = 200,
  DROPDOWN = 400,
  STICKY = 500,
  DRAWER = 600,
  TOAST = 700,
  OVERLAY = 800,
  MODAL = 900,
  MAX = 999,
}
